apiVersion: v1
kind: Service
metadata:
  name: {{ include "first-step.fullname" . }}
  labels:
    {{- include "first-step.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "first-step.selectorLabels" . | nindent 4 }}
